import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.streamMerge',
  appName: 'Stream Merge',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
